# 更新日志

## v0.2.7-beta（2026-09-14）

- 在“查看更早 6 轮”旁增加“查看全部”，允许用户主动一次加载完整历史。
- 默认切换仍只加载最近 6 轮；查看全部只对当前对话生效。

## v0.2.6-beta（2026-09-14）

- 修复私有公网构建在渐进加载调整后丢失飞书授权函数，导致“重新验证”无响应的问题。
- 增加 OAuth 授权入口存在性回归检查，防止历史加载代码调整再次影响登录。

## v0.2.5-beta（2026-09-14）

- 对话首次只传输并显示最近 6 轮，点击“查看更早 6 轮”逐批展开历史。
- 快速切换对话时立即取消旧请求，最后一次点击优先，不再因全局加载锁吞掉点击。
- 后台轮询复用当前已展开的范围，避免静止状态反复下载完整长对话。
- 本地网页与公网网页保持相同逻辑，并补充接口分页和前端切换回归测试。

## v0.2.4-beta（2026-09-07）

- 移除模型选择区的常规说明文字，仅保留错误提示，手机端正常状态只占一行。
- 模型与强度的默认跟随、发送后记忆和运行中追加规则保持不变。

## v0.2.3-beta（2026-09-07）

- 去除跟随默认按钮；未设置会话自动跟随本机默认，手动选择成功发送后才记住。
- 模型标签简化为“模型”，手机端模型和强度并排一行，状态提示单行省略。
- 模型选择草稿不受历史轮询覆盖，也不会在未发送时影响飞书使用的设置。

## v0.2.2-beta（2026-09-07）

- 本地和公网输入区、新建对话弹窗增加模型与推理强度选择，持续显示下一轮使用的设置。
- 默认跟随 Codex 本机保存的模型与强度配置；手动设置按会话记忆，可一键恢复跟随。
- 动态读取账号实际可用模型及对应强度，包括支持模型的 Ultra；不硬编码可用模型。
- 发送时固定模型与强度，后续设置变更不会修改已提交请求；运行中追加引导保持当前回合设置。
- 回复标注桥记录的请求模型和强度，旧记录缺失时明确标为未知；飞书发送回执同步显示请求设置。
- 模型目录刷新不阻塞在线心跳；偏好仅保存到私有运行数据，公开源码不包含用户配置。
- 新增离线回归测试，覆盖默认跟随、按会话记忆、强度校验、消息快照、追加引导和云端鉴权。

## v0.2.1-beta（2026-08-24）

- 多对话运行状态由单一开关升级为活动对话列表：右上角只显示 PC/同步状态，新增“正在运行 N 个”任务入口。
- 输入框上方只跟随当前对话；任务完成提示短暂保留，避免多个任务结束时间不同造成状态来回闪烁。
- 公网在线心跳与完整历史同步解耦，长对话读取超时不再把在线主机误判为“服务中断”。
- 公网状态新增“Codex 正在处理”和“历史同步暂时延迟”，并降低短暂网络抖动造成的错误离线提示。
- 统一本地 PC 网页与公网网页的视觉布局、项目标题、新建弹窗、空状态、输入区域、移动端细节和额度面板。
- 本地 PC 网页补齐“新建对话”：支持现有项目、新建项目和独立会话，创建后自动跳转并显示运行状态。
- 点击本地网页左侧的项目标题，可直接在该项目下创建新对话。
- 网页返回文件按单次回复统一分类：图片、文档、表格、演示文稿、PDF、Markdown 和 TXT 可直接查看或下载。
- 单个可展示文件超过 10 MB，或同一次回复的可展示文件合计超过 30 MB 时，整批转存到 Agent Disk。
- 代码和其他不直接展示的文件不上传，文件卡片改为显示真实本机位置。
- 公网页面使用 7 天自动过期的临时附件副本，并在云端空间不足、接近安全线或上传失败时自动回退到 Agent Disk 并显示原因。
- 本地和公网文件卡片区分“本机原文件、云端临时副本、Agent Disk 副本”，避免将文件名误认为已经上传。

## v0.2.0-beta（2026-08-21）

本次版本集中优化网页输入和多入口协作体验：

- 本地网页增加完整输入区；无公网服务器的用户也能在本机浏览器中继续 Codex 对话。
- 本地网页支持图片和轻量文件，选择附件后可继续填写问题，确认后再发送。
- 待发送附件增加“取消附件”，取消时保留已输入的文字。
- 本地网页、服务器网页和飞书统一并发规则：同一对话中，先到消息开启主任务，后到消息作为补充引导。
- 本地网页持续显示任务状态和阶段性推理摘要，直至任务结束。
- 优化旧桥未加载新接口时的提示，不再直接显示 JSON 解析错误。
- 补充本地网页缓存版本标记，降低更新后仍加载旧前端文件的概率。
- 修复网页新建对话后不自动跳转、左侧列表不及时出现以及刷新后任务状态丢失的问题。
- 新建对话时立即显示创建状态，并在收到新对话 ID 后持续等待历史同步。
- 过滤并拒绝非 UUID 的伪对话条目，避免上传附件后才出现 `invalid thread id` RPC 错误。

## v0.1.0-beta（2026-08-20）

- 首个公开 Beta 测试版。
- 支持通过飞书连接 Windows 主机上的 Codex 对话。
- 提供可选的公网历史查看与网页输入模式。
- 提供 Windows 托盘、开机启动、异常自动恢复和安装向导。
